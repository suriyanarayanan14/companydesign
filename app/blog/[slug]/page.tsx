import Link from 'next/link';import {notFound} from 'next/navigation';import {articles} from '@/lib/articles';export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=articles.find(x=>x.slug===slug);return {title:a?.title,description:a?.summary}}export default async function Article({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=articles.find(x=>x.slug===slug);if(!a)notFound();return <main><section className="page-intro section-pad"><Link href="/blog" className="back-link">← All insights</Link><div className="eyebrow">{a.category}</div><h1>{a.title}</h1><p>{a.summary}</p></section><article className="section-pad prose-page">{a.sections.map(([t,d])=><section key={t}><h2>{t}</h2><p>{d}</p></section>)}<div className="notice">Have a product challenge to explore? <Link href="/contact">Prepare your project brief ↗</Link></div></article></main>}