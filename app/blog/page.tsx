import Link from 'next/link';import {articles} from '@/lib/articles';export const metadata={title:'Insights',description:'Practical notes on product planning, user experience and quality engineering from Cogriss.'};export default function Blog(){return <main><section className="page-intro section-pad"><div className="eyebrow">COGRISS / INSIGHTS</div><h1>Useful thinking.<br/><span>Better decisions.</span></h1><p>Practical notes on planning, designing and delivering digital products.</p></section><section className="section-pad article-grid">{articles.map(a=><article key={a.slug} className="article-card"><div className="eyebrow">{a.category}</div><h2>{a.title}</h2><p>{a.summary}</p><Link className="text-link" href={'/blog/'+a.slug}>Read the article ↗</Link></article>)}</section></main>}