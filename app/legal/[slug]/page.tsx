import {policies} from '@/lib/legal';import {notFound} from 'next/navigation';export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const policy=policies[slug as keyof typeof policies];return {title:policy?.[0]||'Policy',robots:{index:false,follow:false}}}export default async function Policy({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const policy=policies[slug as keyof typeof policies];if(!policy)notFound();const [title,sections]=policy as [string,string[][]];return <main><section className="page-intro section-pad"><div className="eyebrow">COGRISS / PREVIEW POLICY</div><h1>{title}</h1><div className="notice">Draft for this design preview. Business details and live-service terms require completion before commercial launch.</div></section><article className="section-pad prose-page">{sections.map(([t,d])=><section key={t}><h2>{t}</h2><p>{d}</p></section>)}</article></main>}