import {services} from '@/lib/content';import {CTA,FAQs} from '@/components/site';import {notFound} from 'next/navigation';import Link from 'next/link';import {ArrowUpRight} from 'lucide-react';
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(s=>s.slug===(({'mobile-apps':'mobile-app-development'} as Record<string,string>)[slug]||slug));return {title:s?.name||'Service',description:s?.description}}
export default async function Service({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(s=>s.slug===(({'mobile-apps':'mobile-app-development'} as Record<string,string>)[slug]||slug));if(!s)notFound();return <main><section className="page-intro section-pad"><Link className="back-link" href="/services">← All services</Link><div className="eyebrow">{s.eyebrow}</div><h1>{s.name}<span className="title-dot">.</span></h1><p>{s.description}</p><Link href="/contact" className="pill lime">Let’s discuss your project <ArrowUpRight size={20}/></Link></section><section className="detail-section section-pad"><div><div className="eyebrow">WHAT WE CAN BUILD TOGETHER</div><h2>Designed around<br/><span>your goals.</span></h2></div><div className="deliverables">{s.deliverables.map((d,i)=><div key={d}><span>0{i+1}</span><h3>{d}</h3></div>)}<p className="stack">TOOLS WE WORK WITH<br/><strong>{s.stack}</strong></p></div></section><section className="faq-section section-pad"><div><div className="eyebrow">PLANNING YOUR PROJECT</div><h2>What happens<br/><span>next?</span></h2><Link className="text-link" href="/pricing">Explore engagement models ↗</Link></div><FAQs/></section><CTA/></main>}
